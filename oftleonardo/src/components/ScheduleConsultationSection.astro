---
import WhatsAppModal from "@/components/WhatsAppModal";
import { siteConfig } from "@/config/site";

interface Props {
  id?: string;
  title: string;
  description: string;
  whatsappMessageTemplate: string;
  triggerLabel?: string;
  showAgendaCta?: boolean;
}

const {
  id = "agendar",
  title,
  description,
  whatsappMessageTemplate,
  triggerLabel = "Agendar consulta",
  showAgendaCta = true,
} = Astro.props as Props;

---

<section id={id} class="space-y-4 text-center">
  <h2 class="text-xl font-bold tracking-tight sm:text-2xl">
    {title}
  </h2>
  <p class="mx-auto max-w-2xl text-sm leading-relaxed text-muted-foreground">
    {description}
  </p>
  <div class="flex flex-wrap justify-center gap-3">
    <WhatsAppModal
      client:visible
      size="lg"
      className="h-12 rounded-full px-8 text-base font-medium"
      triggerAriaLabel={title}
      whatsappMessageTemplate={whatsappMessageTemplate}
      showOnlineBookingCta={true}
    >
      {triggerLabel}
    </WhatsAppModal>
    {
      showAgendaCta && (
        <a
          href={siteConfig.partnerApps.oftagenda}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex h-12 items-center rounded-full border border-border/70 px-8 text-base font-medium text-foreground transition-colors hover:bg-accent"
        >
          Abrir Minha Agenda
        </a>
      )
    }
  </div>
</section>
